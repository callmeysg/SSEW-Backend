name: Deploy Commerce Service to EC2

on:
  push:
    branches: [ main ]
    paths:
      - 'commerce-service/**'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:
    inputs:
      fresh_deploy:
        description: 'Fresh deployment (removes all data)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

env:
  REGISTRY: docker.io
  IMAGE_NAME: singhtwenty2/ssew-commerce-service

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./commerce-service

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Cache Maven dependencies
        uses: actions/cache@v3
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      - name: Validate code attribution headers
        run: |
          echo "🔍 Validating code attribution headers..."
          mvn license:check -q
          if [ $? -eq 0 ]; then
            echo "✅ All Java files have proper attribution headers!"
          else
            echo "❌ Some files are missing proper attribution headers!"
            echo "💡 Run 'mvn license:format' locally to add missing headers"
            echo "📋 Files without headers:"
            mvn license:check 2>&1 | grep -E "Missing header|File:" || true
            exit 1
          fi

      - name: Build with Maven
        run: mvn clean package -DskipTests

      - name: Generate build number
        id: build-number
        run: echo "BUILD_NUMBER=$(date +%Y%m%d)-${GITHUB_SHA:0:7}" >> $GITHUB_OUTPUT
        working-directory: .

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./commerce-service
          platforms: linux/amd64
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:staging-${{ steps.build-number.outputs.BUILD_NUMBER }}
            ${{ env.IMAGE_NAME }}:staging-latest

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            mkdir -p /home/ubuntu/ssew-deployment
            cd /home/ubuntu/ssew-deployment
            
            curl -o docker-compose.yml https://raw.githubusercontent.com/${{ github.repository }}/main/docker-compose.yml
            
            cat > .env << EOF
            IMAGE_TAG=staging-${{ steps.build-number.outputs.BUILD_NUMBER }}
            AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }}
            AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }}
            JWT_SECRET=${{ secrets.JWT_SECRET }}
            APP_DEVELOPER_SECRET=${{ secrets.APP_DEVELOPER_SECRET }}
            SENTRY_AUTH_TOKEN=${{ secrets.SENTRY_AUTH_TOKEN }}
            POSTGRES_DB=ssew
            POSTGRES_USER=ssew_owner
            POSTGRES_PASSWORD=ssew_staging_pass
            REDIS_PASSWORD=redis_staging_pass
            AWS_REGION=ap-south-1
            AWS_S3_TEMP_BUCKET=ssew-bucket-staging
            AWS_S3_TEMP_FOLDER=temp
            AWS_S3_PERMANENT_FOLDER=permanent
            SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/ssew
            SPRING_DATASOURCE_USERNAME=ssew_owner
            SPRING_DATASOURCE_PASSWORD=ssew_staging_pass
            SPRING_DATA_REDIS_URL=redis://:redis_staging_pass@redis:6379
            EOF
            
            chmod 600 .env
            
            FRESH_DEPLOY="${{ github.event.inputs.fresh_deploy || 'false' }}"
            
            if [ "$FRESH_DEPLOY" = "true" ]; then
              echo "🔥 Fresh deployment requested - removing all data"
              docker-compose down -v
              docker rm -f ssew-postgres ssew-redis ssew-app 2>/dev/null || true
              docker volume rm ssew-deployment_postgres_data 2>/dev/null || true
              docker volume rm ssew-deployment_redis_data 2>/dev/null || true
              echo "✅ All data cleared"
            else
              echo "🔄 Incremental deployment - preserving database data"
              docker-compose stop app
              docker rm -f ssew-app 2>/dev/null || true
            
              if ! docker-compose ps postgres | grep -q "Up"; then
                echo "Starting PostgreSQL..."
                docker-compose up -d postgres
              fi
            
              if ! docker-compose ps redis | grep -q "Up"; then
                echo "Starting Redis..."
                docker-compose up -d redis
              fi
            fi
            
            docker image prune -f
            
            docker-compose pull app
            
            docker-compose up -d app
            
            echo "Waiting for services to be healthy..."
            timeout=300
            while [ $timeout -gt 0 ]; do
              healthy_count=$(docker-compose ps | grep -c "healthy\|Up")
              total_services=$(docker-compose ps | grep -c "ssew-")
            
              if [ "$healthy_count" -eq "$total_services" ]; then
                echo "✅ All services are up and healthy!"
                break
              fi
            
              echo "⏳ Waiting for services... ($timeout seconds remaining)"
              echo "  Healthy: $healthy_count/$total_services"
              sleep 10
              timeout=$((timeout-10))
            done
            
            echo "📊 Service status:"
            docker-compose ps
            
            echo "📋 Application logs:"
            docker-compose logs --tail=20 app
            
            if [ "$FRESH_DEPLOY" = "true" ]; then
              echo "🎉 Fresh deployment completed!"
              echo "📄 Code attribution: All files properly attributed to Aryan Singh (@singhtwenty2)"
            else
              echo "🎉 Incremental deployment completed!"
              echo "📄 Code attribution: All files properly attributed to Aryan Singh (@singhtwenty2)"
            fi